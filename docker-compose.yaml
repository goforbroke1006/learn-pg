version: '3'

services:
  postgres:
    image: postgres:10
    environment:
      POSTGRES_USER:      demo
      POSTGRES_PASSWORD:  demo
      POSTGRES_DB:        demo
    ports:
      - "5432:5432"
    volumes:
      - ./migrations/:/docker-entrypoint-initdb.d/